# 現場記録アプリ (Construction Camera App) 開発ログ

このファイルは、AIアシスタント(Antigravity)と共に開発した機能と履歴を保存しておくための記録です。
今後の機能追加や改修の際に、このファイルを見返すことでスムーズに開発を再開できます。

## 📅 開発履歴まとめ (2026年4月)

### 1. 基本UIとカメラ機能の構築
*   **Next.js (App Router)** を使用した基盤構築。
*   `react-webcam` を用いたカメラ撮影機能の実装。
*   現場で使いやすいように「工事内容」「履歴」「カメラ撮影」の3つのタブ構成に最適化。
*   縦横の画面回転（レスポンシブ）への自動対応。
*   カメラのズーム機能（スライダーおよび標準カメラ呼び出し）の実装。

### 2. 黒板（情報合成）機能
*   **標準黒板:** 工事名、会社名、日時をカメラ映像の右下にリアルタイムで黒板風に合成。
*   **図面・寸法入り黒板（大）:** 標準の2倍サイズの黒板。手入力の「寸法」に加え、Googleドライブから選択した画像（図面・ひな形）を黒板内に合成可能。
*   iOS端末での高画質撮影時に発生する「真っ黒になるバグ」を回避するため、Canvasでの画像圧縮・ダウンサンプリング処理（最大1280px）を実装。

### 3. Google API連携（NextAuth + Googleapis）
*   **Google OAuth2 認証:** `next-auth` を使用し、アプリ内でGoogleアカウントにログイン。
*   **トークン自動更新:** アクセストークン切れ（1時間）を防ぐため、`refresh_token` を用いた自動更新ロジックを実装。
*   **Google Drive連携:**
    *   撮影した合成写真をドライブに自動アップロード。
    *   `電気仕事 > 工事記録 > [工事名] > [西暦] > [月]` の階層フォルダを自動作成し整理。
    *   `電気仕事 > 工事記録 > 黒板` フォルダから、図面用画像（JPEG/PNG）を読み込んでアプリに表示。
*   **Google Calendar連携:**
    *   写真保存時に、Googleカレンダーへ自動で「予定（現場記録）」を登録。
    *   予定の説明欄に、工事内容、Excelファイルのリンク、および「Google Maps」「OpenStreetMap」のリンクを記載。

### 4. Excel (エクセル) 生成機能
*   `exceljs` パッケージを使用。
*   ドライブにアップロードした画像のURL（Base64変換）をExcelシートに貼り付け。
*   工事名、会社名、日時などのメタデータをシート内に記載し、`.xlsx` 形式でGoogleドライブへ直接保存。

### 5. マップ・位置情報機能
*   ブラウザの `Geolocation API` を使用して現在地の緯度・経度を取得。
*   Googleの逆ジオコーディングAPIで住所のテキストを取得。
*   **OpenStreetMap (Leaflet):** `react-leaflet` を使用し、アプリの「工事内容」タブ内に地図を表示。
*   地図上に黄色のカスタムピン（ラベルに工事名を表示）を立てる機能を実装。

### 6. Vercelへの本番公開とアプリ化
*   Vercel CLI を用いて、アプリを無料クラウドサーバーにデプロイ（`https://construction-camera-app.vercel.app`）。
*   スマホの「ホーム画面に追加」機能に対応させるため、電柱と信号機をモチーフにしたフラットデザインの専用アプリアイコン（`icon.png`, `apple-icon.png`）をAI生成し設定。
*   **横画面（ランドスケープ）対応:** スマホを横向きにして撮影する際、シャッターボタンやズーム等の操作パネルが画面右側に並び、スクロールなしで快適に撮影できるレイアウト調整を実施。
*   **黒板表示ON/OFF機能:** 写真のみを撮影したいケースに対応するため、カメラ画面からワンタッチで黒板の合成を非表示にするトグルボタンを追加。

---

## 🛠 今後の起動・更新手順（自分用メモ）

1. **アプリのURL（本番用）:**
   👉 `https://construction-camera-app.vercel.app`

2. **アプリの更新（再アップロード）手順:**
   コードを修正した後に最新版をVercelに公開するには、パソコンのターミナルで以下のコマンドを実行します。
   ```bash
   npx vercel --prod --yes
   ```

2. **iPhoneからアクセスするためのCloudflare設定:**
   別のターミナルを開き、以下のコマンドでiPhone用のHTTPS URLを発行します。
   ```bash
   cloudflared tunnel --url http://localhost:3000
   ```
   ターミナルに表示される `https://xxxxxx.trycloudflare.com` というURLをiPhoneのSafariで開きます。

3. **認証エラーが出た場合:**
   Googleの認証トークンが切れている、または更新に失敗している可能性があります。iPhoneの画面右上から一度「ログアウト」し、再度「ログイン」し直すことで解決します。
